<project name="publish">
	
	<target description="Publish JARs to an online storage."
		name="publish" 
		depends="jar"
		if="env.CI">

		<exec executable="./.infra/build/bash/publish" failonerror="true">
			<arg line="./bin/*/jar/*.jar"/>
		</exec>

	</target>

</project>
